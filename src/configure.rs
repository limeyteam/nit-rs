use std::env;
use std::fs;
use toml::Value;
/*
pub fn deser_config() {
    let filename = "nit.toml";

    let contents = fs::read_to_string(filename)
        .expect("Something went wrong reading the file");

    let tomltable = contents.unwrap();
    println!("{:?}", tomltable["author"]);
}
*/